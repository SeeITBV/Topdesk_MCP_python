# Natural Language → TOPdesk MCP Router Configuration

# Connection Mode 1: Via MCP Server (Recommended)
MCP_BASE_URL=http://localhost:3030
MCP_API_KEY=your-api-key-if-needed

# Connection Mode 2: Direct TOPDESK Connection (Alternative)
# Uncomment and configure these to connect directly to TOPDESK instead of via MCP server
# TOPDESK_URL=https://yourcompany.topdesk.net
# TOPDESK_USERNAME=your_username
# TOPDESK_PASSWORD=your_api_token

# Optional LLM Provider (for future enhancement)
LLM_PROVIDER=openai

# Logging Configuration
LOG_LEVEL=INFO

# Rate Limiting (defaults shown)
RATE_LIMIT_REQUESTS=60
RATE_LIMIT_WINDOW=300

# Circuit Breaker (defaults shown)
CIRCUIT_BREAKER_FAILURE_THRESHOLD=5
CIRCUIT_BREAKER_RECOVERY_TIMEOUT=60

# Request Configuration (defaults shown)
MCP_TIMEOUT=8
MCP_RETRIES=2
DEFAULT_MAX_RESULTS=5
MAX_ALLOWED_RESULTS=25
DEFAULT_TIME_WINDOW=30